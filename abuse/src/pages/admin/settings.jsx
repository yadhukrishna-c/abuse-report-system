import React from "react";

const Settings = () => {
  return (
    <div className="settings-container">
      <div className="settings-card">
        <h2>Settings</h2>
        <p>Manage platform preferences, access controls, and notification defaults.</p>
      </div>
    </div>
  );
};

export default Settings;
